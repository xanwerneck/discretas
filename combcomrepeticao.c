

#include <stdio.h>
#include <string.h>
#include <stdlib.h>


static int  dimTam = 4 ;

void CombComRepeticao( char * alfabeto);

int main ( int argc, char *argv[] 	 )
{
   char alfabeto [4] = {'c','a','s','a'};
   CombComRepeticao(alfabeto);
} 

void CombComRepeticao(char * alfabeto)
{
	int i;
	if (alfabeto)
	{
		/* code */
	}
	else
	{
		char * nvo = Distintos(alfabeto)
		for (i = 0; i < count; ++i)
		{
			/* code */
		}
	}
}

char * Distintos(alfabeto)
{
	return alfabeto;
}
